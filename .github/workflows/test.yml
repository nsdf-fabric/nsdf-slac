name: Test

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - edited

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v4

      - name: Hello
        uses: dagger/dagger-for-github@8.0.0
        with:
          call: test-nsdf-library
          version: "0.18.14"
